// Launcher — entry point
// Open the update window and, when ready, start ProcessSuspender.exe.

Application.EnableVisualStyles();
Application.SetCompatibleTextRenderingDefault(false);
Application.Run(new LauncherForm());
